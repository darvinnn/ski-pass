import { Typography, styled } from '@mui/material';

const UserAgeText = styled(Typography)({
  color: 'rgba(0, 0, 0, 0.54)',
  fontSize: '14px',
  fontWeight: '400',
  lineHeight: '20px',
});

export default UserAgeText;
