import { Typography, styled } from '@mui/material';

const UserProfession = styled(Typography)({
  fontSize: '10px',
  fontWeight: '400',
  color: '#707EF9',
  marginTop: '-2px',
});

export default UserProfession;
