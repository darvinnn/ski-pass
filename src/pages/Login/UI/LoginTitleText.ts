import { Typography, styled } from '@mui/material';

const LogitTitleText = styled(Typography)({
  fontSize: '18px',
  fontWeight: '700',
  textTransform: 'uppercase',
  textAlign: 'center',
  lineHeight: '16px',
});

export default LogitTitleText;
