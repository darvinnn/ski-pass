import MainInfo from '../../components/MainInfo/MainInfo';
import BasicBlock from '../../components/BasicBlock/BasicBlock';

const Users = () => {
  return (
    <MainInfo>
      <BasicBlock size="big" type="users" />
    </MainInfo>
  );
};

export default Users;
