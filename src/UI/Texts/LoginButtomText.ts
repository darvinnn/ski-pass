import { Typography, styled } from '@mui/material';

const LoginButtomText = styled(Typography)({
  fontSize: '12px',
  color: '#1371FA',
  textDecoration: 'underline',
  marginTop: '27px',
  lineHeight: '14px',
});

export default LoginButtomText;
