import { Typography, styled } from '@mui/material';

const SkiPassTimeText = styled(Typography)({
  color: '#fff',
  fontSize: '14px',
  fontWeight: '700',
  lineHeight: '16.41px',
});

export default SkiPassTimeText;
