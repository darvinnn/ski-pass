import { Typography, styled } from '@mui/material';

const UserName = styled(Typography)({
  fontSize: '12px',
  fontWeight: '500',
  color: '#fff',
  marginTop: '12px',
});

export default UserName;
